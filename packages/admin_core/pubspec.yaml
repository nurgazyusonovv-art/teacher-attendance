name: admin_core
description: >-
  Shared administration models and repositories for the teacher and web admin
  apps. Both surfaces talk to the same API; only their UI differs.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.3

dependencies:
  dio: ^5.8.0+1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
